# Best Previous Horse — V73

This is the strongest horse attempt retained from the pre-cleanup revisions.
It is reference-only and intentionally stored outside the Godot project so it cannot add runtime or import overhead.

The production horse derives its silhouette from this revision but adds actual vertex weights and deforming `Idle`/`Trot` animation support.
